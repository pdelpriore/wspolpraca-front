const getUserType = (): Promise<string> =>
  new Promise((resolve, reject) => {
    // show userTypeSnackbar
    // get userType from snackbar form by reactive var
    // if userType hide userTypeSnackbar
    // resolve the user type
    // if cancel snackbar form
    // reject
  });

export default getUserType;
